/*#include <stdio.h>

int main(void) {
	int x, y, temp;
	int max, min;
	x = 50; y = 43;

	//기본
	if (x > y) {
		printf("%d %d", x, y);
	}
	else {
		printf("%d %d", y, x);
	}

	return 0;

	//printf문을 1번 사용
	if (x < y) {
		temp = x;
		y = x;
		y = temp;
	}
	printf("%d %d", x, y);

	//max 변수와 min 변수 사용
	if (x > y) {
		max = x; min = y;
	}
	else {
		max = y; min = x;
	}
	printf("%d %d", max, min);
}*/